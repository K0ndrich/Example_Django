# Файл хранит настройки для одного текущего docker-файла

# FROM берем образ на котором будет работать текущий один docker-контейнер
FROM python:latest

# берем все файлы из нашего проекта и помещаем в docker-контейнера
# ADD откуда - куда
ADD . .

# CMD указывает действия которые будут делаться при запуске docker-контейнера
CMD python test.py 



